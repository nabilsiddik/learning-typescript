 // Creating types
 
 // Function type
   // Normal function
   function fn (input: number): void | null | number {
    const multi = input * 2
    return input
 }

 // Arrow Function
 const fnAgain = (input: number): void | null | number => {
    const multi = input * 2
    return input
 }
